{
  "name": "autotester",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test-login": "cypress run --config-file cypress.login.json --headed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^8.5.0"
  }
}